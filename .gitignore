# Supabase (CLI)
supabase/.branches
supabase/.temp
supabase/.env

# Entorno — .env SÍ se versiona (solo valores públicos VITE_*).
# Los secretos (service_role, etc.) van en .env.local, nunca versionado.
.env.local
.env.*.local

# Dependencias / build
node_modules/
dist/
.next/
out/
*.tsbuildinfo
.vercel

# SO / editor
.DS_Store
Thumbs.db
